def problemVersion = '0.24.0'

dependencies {
    implementation project(":fetch-doi-commons")
    implementation project(":doi-transformer")
    implementation project(":doi-proxy")

    compileOnly group: 'jakarta.ws.rs', name: 'jakarta.ws.rs-api', version: project.ext.jakartaVersion
    testImplementation group: 'jakarta.ws.rs', name: 'jakarta.ws.rs-api', version: project.ext.jakartaVersion

    testImplementation(group: 'org.zalando', name: 'problem', version: problemVersion) {
        compileOnly(group: 'org.apiguardian', name: 'apiguardian-api', version: '1.1.0') {
            because 'Dependency has maven scope provided'
        }
    }
}