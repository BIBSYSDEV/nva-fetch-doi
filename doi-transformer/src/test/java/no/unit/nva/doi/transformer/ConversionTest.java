package no.unit.nva.doi.transformer;

import java.net.URI;
import java.util.UUID;

public class ConversionTest {

    public static final String SAMPLE_CROSSREF_FILE = "crossref.json";
    protected static final UUID SOME_UUID = UUID.randomUUID();
    private static final String SOME_PUBLISHER = "www.publisher.com";
    public static final URI SOME_PUBLISHER_URI = URI.create(SOME_PUBLISHER);
}
